# dependencies
node_modules
.pnp
.pnp.js

# builds
.next
out
dist
build
*.tsbuildinfo

# env / secrets — never commit
.env
.env.local
.env.*.local
.env.development
.env.production
*.pem
*.key
pcsexpress-upload-key.jks

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.log

# os / editor
.DS_Store
.vscode/*
!.vscode/extensions.json
.idea

# testing / coverage — anchor to repo root so we don't swallow
# `apps/web/src/app/coverage/` (the public /coverage status dashboard).
/coverage
**/coverage-report
.nyc_output

# prisma
packages/db/prisma/migrations/dev.db
packages/db/prisma/dev.db*

# vercel
.vercel

# turborepo / next telemetry
.turbo
next-env.d.ts
.env*.local

# Prisma 7 — per-consumer generated clients (regenerated at build/install time
# from packages/db/prisma/schema.prisma; see each app's build + postinstall).
packages/db/src/generated/
apps/api/src/generated/
apps/web/src/generated/
.env*
